// const express = require("express");
// const router = express.Router();
// const controller = require("../controllers/user");

// // Creating a list
// router.post("/lists", controller.createList);

// // Updating a list
// router.patch("/lists/:id", controller.updatedCategory);

// //Getting all lists
// router.get("/lists", controller.getAllCategories);

// //Get a list
// router.get("/lists/:id", controller.getCategory);

// // Delete a list
// router.delete("/lists/:id", controller.deletedCategory);

// module.exports = router;
